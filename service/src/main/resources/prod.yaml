spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    root: INFO
    org:
      springframework:
        web: WARN
      hibernate:
        SQL: WARN
springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false
